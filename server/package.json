{
  "name": "career-sim-boards",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "schema": "wrangler d1 execute career-sim-boards --remote --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260905.1",
    "typescript": "~7.0.2",
    "wrangler": "^4.129.0"
  },
  "overrides": {
    "sharp": "^0.35.4"
  }
}
